<% if @video.video.attached? %>
  <video src="<%= rails_blob_path(@video.video) %>" type="video/mp4" controls></video>
  <p><%= @video.title %></p> <br>
  <p><%= @video.content %></p>
<% end %>

<!--%= render 'comments/comment', video: @video, comment: @comment %>-->

<%= link_to "コメントする", user_video_comments_path(@video) %>
